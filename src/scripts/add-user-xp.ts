'use server';

import { prisma } from '@/lib/prisma';

/**
 * A method that fetches all users answers and maps the correct answers to the user xp
 *
 * @param userId
 */
export const addUserXp = async (userId: string) => {};
